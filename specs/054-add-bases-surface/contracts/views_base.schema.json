{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "views_base",
  "description": "Input and output schemas for the views_base tool (BI-0082). Active-mode-only (R-003).",

  "input": {
    "type": "object",
    "properties": {
      "vault": {
        "type": "string",
        "minLength": 1,
        "description": "Vault display name for multi-vault routing. Note: silently ignored by current CLI (R-003)."
      }
    },
    "additionalProperties": false
  },

  "output": {
    "type": "object",
    "properties": {
      "views": {
        "type": "array",
        "items": { "type": "string" },
        "description": "View names defined in the focused .base file"
      },
      "count": {
        "type": "integer",
        "minimum": 0,
        "description": "Number of views found (always equals views.length)"
      }
    },
    "required": ["views", "count"],
    "additionalProperties": false
  }
}
