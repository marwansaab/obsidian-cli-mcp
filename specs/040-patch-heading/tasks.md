---

description: "Task list for patch_heading (BI-040) — dependency-ordered, organised by user story."
---

# Tasks: Patch Heading

**Input**: Design documents from `/specs/040-patch-heading/` — [spec.md](spec.md), [plan.md](plan.md), [research.md](research.md), [data-model.md](data-model.md), [contracts/](contracts/), [quickstart.md](quickstart.md).
**Prerequisites**: plan.md ✓, spec.md ✓, research.md ✓, data-model.md ✓, contracts/ ✓.

**Tests**: Co-located `*.test.ts` unit tests are MANDATORY per Constitution Principle II (every MCP tool ships with happy-path + failure-or-boundary tests in the same change that adds it). Tests are NOT optional. T0 live-vault scenarios from quickstart.md run at `/speckit-implement` time per CLAUDE.md `## Test Execution`.

**Organisation**: Tasks are grouped by user story. Setup + Foundational (Phase 1 + 2) build the shared Zod schemas, the pure helpers (`heading-walk.ts`, `body-edit.ts`), and the module scaffold. User stories then layer the handler logic incrementally — US1 ships the happy-path MVP for `target_mode: "specific"` (all three placement modes, fs read-modify-write, registration); US2 layers in the diagnostic-error classification (HEADING_NOT_FOUND, HEADING_RACE); US3 adds active-mode focused-file resolution; US4 adds external-editor conflict detection.

## Format: `[ID] [P?] [Story?] Description`

- **[P]**: Can run in parallel — different files, no dependencies on incomplete tasks.
- **[Story]**: User story this task belongs to (US1, US2, US3, US4).
- File paths absolute or repo-rooted.

## Path Conventions

Single-project layout under `src/`. Test files co-located alongside source per Principle II — `src/tools/patch_heading/handler.ts` ↔ `src/tools/patch_heading/handler.test.ts`. The module ships five production files + five co-located test files per [plan.md §Project Structure](plan.md).

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Scaffold the new per-surface module directory and prepare the docs placeholder.

- [ ] T001 Create the new tool module directory at [src/tools/patch_heading/](src/tools/patch_heading/) and seed each file with an `// Original — no upstream.` one-line header per Constitution Principle V. Files to create as header-only stubs: `schema.ts`, `heading-walk.ts`, `body-edit.ts`, `handler.ts`, `index.ts`. Co-located test files `schema.test.ts`, `heading-walk.test.ts`, `body-edit.test.ts`, `handler.test.ts`, `index.test.ts` get header-only stubs too — tests are filled in by later tasks.
- [ ] T002 [P] Create the docs placeholder at [docs/tools/patch_heading.md](docs/tools/patch_heading.md) — a header-only stub that is filled in during the Polish phase per ADR-005 progressive-disclosure convention.

**Checkpoint**: Module directory exists; every file carries its attribution header. No production logic yet.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Build the Zod schemas and the two pure helpers that every story consumes. Schema is the single source of truth for the published MCP `inputSchema` AND the runtime parse per Constitution Principle III. The pure helpers carry all algorithmic logic (heading walk + race-identity + body splice) and are unit-tested in isolation before any handler integration.

**⚠️ CRITICAL**: No user story task can begin until this phase is complete. Foundational covers FR-001 / FR-002 / FR-003 / FR-004 / FR-005 / FR-006 / FR-009 / FR-010 / FR-011 / FR-012 / FR-013 / FR-014 / FR-015 / FR-016 / FR-018 / FR-018a at the schema + pure-helper layer.

- [ ] T003 [P] Implement the input + output Zod schemas in [src/tools/patch_heading/schema.ts](src/tools/patch_heading/schema.ts) per [data-model.md §Input](data-model.md). **Input schema**: built on `targetModeBaseSchema` + `applyTargetModeRefinement` from [src/target-mode/target-mode.ts](src/target-mode/target-mode.ts) (cohort parity with `write_note`). Extended fields: `file: safePathField.optional()`, `path: safePathField.optional()` (both gated by `isStructurallySafePath` from [src/path-safety/schema.ts](src/path-safety/schema.ts)); `heading_path: z.string().min(1).max(1000)` with `superRefine` running the segment-split-and-validate logic (split on literal `#`, surface `INVALID_HEADING_PATH` with `details.reason ∈ {"empty", "empty-segment", "contains-hash", "single-segment", "too-long"}`); `mode: z.enum(["append", "prepend", "replace"])`; `content: z.string()`. Add a top-level `superRefine` that rejects empty `content` when `mode ∈ {"append", "prepend"}` by emitting `ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["content"], message: "...", params: { code: "EMPTY_CONTENT", reason: input.mode, mode: input.mode } })` per the cohort's custom-issue pattern (parity with [src/tools/write_note/schema.ts](src/tools/write_note/schema.ts)'s active-mode `overwrite` superRefine). The boundary translator at `_register.ts` / handler-side maps the Zod custom issue's `params` carry into the resulting `UpstreamError`'s `code: "VALIDATION_ERROR"` + `details.code: "EMPTY_CONTENT"` + `details.reason: "append" | "prepend"` envelope. Strict mode (`.strict()`) is inherited from the target-mode primitive. Export `z.infer<typeof patchHeadingInputSchema>` as `PatchHeadingInput`. **Output schema**: `.strict() z.object` with `path: z.string()`, `vault: z.string()`, `heading_path: z.string()`, `mode: z.enum(["append", "prepend", "replace"])`, `bytes_written: z.number().int().min(0)`. Export `z.infer<typeof patchHeadingOutputSchema>` as `PatchHeadingOutput`. Per Principle III, schemas are the single source of truth — no parallel TypeScript interfaces for the input or output shapes.
- [ ] T004 [P] Implement the heading walk pure helper in [src/tools/patch_heading/heading-walk.ts](src/tools/patch_heading/heading-walk.ts) per [data-model.md §Heading walk algorithm](data-model.md). Exports: (a) `parseHeadingPath(headingPath: string): string[]` — splits on literal `#`; pure, deterministic. (b) `walkHeadings(content: string, segments: string[]): ResolvedHeading | null` — single-pass line-by-line ATX scanner with fenced-code opacity (`inFence: boolean` toggled on any line whose lstripped form begins with ≥ 3 consecutive `` ` `` or `~` per research.md R3); ATX-only per research.md R2 (setext NOT recognised); segment-by-segment walk with parent-chain bookkeeping (most-recently-seen heading at `rank - 1` since the previous heading of `rank ≤ rank - 1`); returns first-match-wins per FR-006. The returned `ResolvedHeading` carries `markerLineIndex`, `markerLineText`, `rank`, `parentChainText` (segments-joined-with-`#`, excluding the leaf), `reachStartLineIndex`, `reachEndLineIndex` (one past the next equal-or-higher-rank heading or `lines.length`), `directBodyEndLineIndex` (one past the first child heading or equal to `reachEndLineIndex` if no child). Returns `null` on resolution failure. (c) `resolveHeadingIdentity(resolved: ResolvedHeading): HeadingIdentity` — pure 3-tuple `{ markerLineText, rank, parentChainText }` extraction per research.md R4. Pure functions; no fs access, no `UpstreamError`-throwing; the handler converts `null` to the typed error.
- [ ] T005 [P] Implement the body edit pure helper in [src/tools/patch_heading/body-edit.ts](src/tools/patch_heading/body-edit.ts) per [data-model.md §Body-edit algorithm](data-model.md). Exports: `applyAppend(lines: string[], resolved: ResolvedHeading, content: string): string[]` (insert content as splice immediately before `reachEndLineIndex`); `applyPrepend(lines: string[], resolved: ResolvedHeading, content: string): string[]` (insert content immediately after `markerLineIndex`); `applyReplace(lines: string[], resolved: ResolvedHeading, content: string): string[]` (replace the slice `[reachStartLineIndex, directBodyEndLineIndex)` with content split-on-`\n`). Also export `detectLineEnding(rawContent: string): "lf" | "crlf"` and `detectTrailingNewline(rawContent: string): boolean` so the handler can preserve the conventions per FR-014 + FR-015. The content string is inserted as-is split on `\n`; the helper does NOT add or strip newlines from the supplied content. Pure functions; no fs access.
- [ ] T006 Implement the schema-cohort tests in [src/tools/patch_heading/schema.test.ts](src/tools/patch_heading/schema.test.ts). Cases: happy-path validation of `{ target_mode: "specific", vault: "Knowledge", path: "Daily Notes/2026-05-21.md", heading_path: "Daily#Tasks#TODO", mode: "append", content: "- new item\n" }`; happy-path with `file:` instead of `path:`; happy-path with `target_mode: "active"` and no vault/file/path; **INVALID_HEADING_PATH cohort** — empty `heading_path` → `details.reason: "empty"`; `"Tasks"` (one segment) → `"single-segment"`; `"Top##Sub"` (empty middle segment) → `"empty-segment"`; `"#Top#Sub"` (leading `#`) → `"empty-segment"`; `"Top#Sub#"` (trailing `#`) → `"empty-segment"`; 1001-char `heading_path` → `"too-long"` with `details.value_length: 1001`; **EMPTY_CONTENT cohort** — `mode: "append"` + `content: ""` → `details.code: "EMPTY_CONTENT" + details.reason: "append" + details.mode: "append"`; `mode: "prepend"` + `content: ""` → `"prepend"`; `mode: "replace"` + `content: ""` → ACCEPTED (no error fires); **target-mode interaction** — `target_mode: "specific"` without `vault` → cohort target-mode error; `target_mode: "active"` with `vault` → cohort target-mode error; `target_mode: "specific"` with both `file` and `path` → cohort target-mode error; **misc** — unknown top-level field → `VALIDATION_ERROR` with Zod `unrecognized_keys`; missing required `heading_path` → Zod issue path `["heading_path"]`; missing required `mode` → `["mode"]`; missing required `content` → `["content"]`; non-string `heading_path` → Zod `invalid_type`; `mode` outside enum → Zod `invalid_enum_value`. **Output schema**: accepts a well-formed envelope; rejects unknown top-level keys (strict mode); rejects negative `bytes_written`; rejects non-integer `bytes_written`. Depends on T003.
- [ ] T007 Implement the heading-walk pure-function tests in [src/tools/patch_heading/heading-walk.test.ts](src/tools/patch_heading/heading-walk.test.ts). Cases: **parseHeadingPath** — `"Top#Sub"` → `["Top", "Sub"]`; `"Top#Sub#Leaf"` → 3 segments; trailing `#` produces trailing empty segment (caught by schema layer, but the parser preserves it); single segment returns 1-element array. **walkHeadings happy paths** — simple 2-segment resolve (`"Top#Sub"` against a note with `# Top` then `## Sub`); 3-segment resolve with multiple siblings (`"Top#A#X"` against `# Top / ## A / ### X / ### Y / ## B`); ATX rank 1 / 2 / 6 (heading marker lengths 1 / 2 / 6 `#` characters); heading text with special characters (`"Top#Sub & Co."`, `"Top#2026/05/21"`, `"Top#Tasks (P1)"`). **walkHeadings failure paths** — leaf not found → `null`; ancestor not found → `null`; right-text-wrong-parent (`"Top#Sub"` against `# Other / ## Sub`) → `null` (the ancestor's text must match exactly per FR-001); right-rank-wrong-parent for a leaf that exists at a different ancestor → `null`. **First-match-wins (FR-006)** — note with `# Top / ## Sub / body A / ## Sub / body B`; `"Top#Sub"` resolves to the FIRST `## Sub` (markerLineIndex points to the earlier occurrence). **Fenced-code opacity (FR-013)** — note with a fenced block containing `# Pseudo-heading inside fence`; walker does NOT recognise that line as a heading; the surrounding scan resumes correctly after the fence closes. Both `` ``` `` and `~~~` fences tested. **ATX-only (research.md R2)** — note with a setext heading (text followed by `===` underline); walker treats both lines as ordinary body, does not recognise setext as a heading. **resolveHeadingIdentity** — extracts `(markerLineText, rank, parentChainText)`; two identities with byte-identical fields compare equal; one differing field produces inequality. **Reach boundaries** — `reachEndLineIndex` points one past the next equal-or-higher-rank heading; `directBodyEndLineIndex` points one past the first child heading (or equals `reachEndLineIndex` if no child). Depends on T004.
- [ ] T008 Implement the body-edit pure-function tests in [src/tools/patch_heading/body-edit.test.ts](src/tools/patch_heading/body-edit.test.ts). Cases: **applyAppend** — body with content followed by a sibling heading → content lands immediately before the sibling marker line; body-with-child-subtree → content lands at end of reach (after the child subtree, immediately before the next equal-or-higher-rank heading); empty body → content lands immediately after marker line (the "end of empty reach" position); last-heading-in-note → content lands at end-of-lines. **applyPrepend** — body with content → content lands immediately after marker line, before existing body; marker line immediately followed by child heading → content lands between the two markers (verifying FR-011's adjacent-child case). **applyReplace** — body with content + no child subtree → marker preserved, body swapped, next equal-or-higher-rank heading preserved; body with child subtree → marker preserved, direct body swapped, child subtree preserved unchanged; empty body → marker preserved, content lands as the new direct body; **empty content for replace** (FR-018a) → direct body becomes zero lines, marker + child subtree preserved. **Line-ending preservation (FR-015)** — CRLF input → output uses CRLF on both modified-region boundaries and elsewhere; LF input → output uses LF throughout. `detectLineEnding` returns `"lf"` for no-CRLF, `"crlf"` when any CRLF is present. **Trailing-newline preservation (FR-014)** — file ends with `\n` → output ends with `\n`; file does not end with `\n` → output does not. `detectTrailingNewline` correctly identifies both. **Content with embedded newlines** — multi-line content splits on `\n` and lands as multiple lines correctly across all three modes. **No-modify guarantee** — for each mode, every line outside the modified region is byte-identical to the input. Depends on T005.

**Checkpoint**: Schemas ship with full validation-error cohort coverage. The two pure helpers (`heading-walk.ts`, `body-edit.ts`) ship with full algorithmic coverage. `handler.ts` and `index.ts` are still header-only stubs. Tests pass `vitest run`. Every error envelope listed in [contracts/errors.md](contracts/errors.md) under the validation-layer section is asserted. Every walk + edit invariant from [data-model.md](data-model.md) is asserted.

---

## Phase 3: User Story 1 - Surgical placement under a named heading (Priority: P1) 🎯 MVP

**Goal**: Ship the happy-path MVP. Caller invokes the tool with `target_mode: "specific"` + `vault` + `file`/`path` + `heading_path` + `mode` + `content` and the wrapper performs the fs read-modify-write through the ADR-009 substrate. All three placement modes (append, prepend, replace) land correctly; line-ending and trailing-newline conventions are preserved; the success envelope echoes the locator. Includes registration through the boot spine so the tool is callable from MCP clients.

**Independent Test**: Run quickstart.md examples 1–4 against the authorised test vault — append against a TODO sublist, prepend a lead-in under Notes, replace the Done sublist's body, clear a body via replace-with-empty. Verify each response shape matches the envelope contract exactly AND the post-edit file's bytes outside the targeted region are unchanged from the pre-call state.

This MVP includes the full happy-path fs read-modify-write for `target_mode: "specific"`, the registration tail (factory + server wire + baseline update), and the metadataCache invalidation eval per ADR-009. It does NOT include the diagnostic-error classification (US2 covers HEADING_NOT_FOUND + HEADING_RACE), active-mode focused-file resolution (US3), or external-editor conflict detection (US4).

### Implementation for User Story 1

- [ ] T009 [US1] Implement the `ExecuteDeps` shape and the handler skeleton in [src/tools/patch_heading/handler.ts](src/tools/patch_heading/handler.ts). Deps interface: `{ logger: Logger, queue: Queue, spawnFn?: SpawnLike, env?: NodeJS.ProcessEnv, fs: { readFile: typeof import("node:fs/promises").readFile, writeFile: typeof import("node:fs/promises").writeFile, rename: typeof import("node:fs/promises").rename, realpath: typeof import("node:fs/promises").realpath }, vaultRegistry: VaultRegistry }` (cohort-uniform with [src/tools/write_note/handler.ts](src/tools/write_note/handler.ts)'s shape — every fs primitive injected via deps so tests can mock them). Export `executePatchHeading(input: PatchHeadingInput, deps: ExecuteDeps): Promise<PatchHeadingOutput>`. Body is a stub that throws "not implemented" — populated by T010–T015. Imports: `parseHeadingPath`, `walkHeadings`, `resolveHeadingIdentity` from `./heading-walk.js`; `applyAppend`, `applyPrepend`, `applyReplace`, `detectLineEnding`, `detectTrailingNewline` from `./body-edit.js`; types + schemas from `./schema.js`; `UpstreamError` from `../../errors.js`; `checkCanonicalPath` from `../../path-safety/canonical.js`; types from `../../logger.js` + `../../queue.js` + `../../vault-registry/index.js`.
- [ ] T010 [US1] Implement specific-mode locator resolution in handler.ts. Branch on `input.target_mode === "specific"`. Resolve `vaultRoot = await deps.vaultRegistry.resolveVaultPath(input.vault)` (the existing helper surfaces `VAULT_NOT_FOUND` automatically). Build `relPath = input.file ?? input.path` (exactly one is present per schema's target-mode primitive). Active-mode branch (T024) is filled in by US3. Depends on T009.
- [ ] T011 [US1] Implement the Layer 2 path-safety check in handler.ts per ADR-009 §2 / FR-007 cohort. Call `check = await checkCanonicalPath(vaultRoot, relPath, { realpath: deps.fs.realpath })`. On `{ ok: false }`: emit `deps.logger.pathEscapeAttempt({ vault: input.vault ?? null, attemptedPath: check.attemptedPath })`; throw `UpstreamError` with `code: "PATH_ESCAPES_VAULT"` + `details.vault: input.vault ?? null` + `details.attemptedPath: check.attemptedPath` + `details.resolvedPath: check.resolvedPath`. On `{ ok: true }`: capture `absPath = check.resolvedPath` for subsequent fs operations. Depends on T010.
- [ ] T012 [US1] Implement the file read + heading resolve in handler.ts. Call `rawBytes = await deps.fs.readFile(absPath, "utf8")` inside a try/catch — on `ENOENT` throw `UpstreamError` with `code: "FS_WRITE_FAILED"` + `details.errno: "ENOENT"` + `details.path: relPath` + message naming "note file not found" (FS_WRITE_FAILED is reused for non-existent target since FR-022 forbids file creation by patch_heading). On success: capture `originalContent = rawBytes`; detect `lineEnding = detectLineEnding(originalContent)`, `trailingNewline = detectTrailingNewline(originalContent)`; split into `lines = originalContent.split(lineEnding === "crlf" ? /\r\n/ : "\n")`. Call `segments = parseHeadingPath(input.heading_path)`; call `resolved = walkHeadings(originalContent, segments)`. If `resolved === null`, throw `UpstreamError` with `code: "CLI_REPORTED_ERROR"` + `details.code: "HEADING_NOT_FOUND"` + `details.heading_path: input.heading_path` + `details.path: relPath` (the full HEADING_NOT_FOUND treatment is exercised by US2's tests; the throw site lives here in US1). Compute `originalIdentity = resolveHeadingIdentity(resolved)`. Depends on T011.
- [ ] T013 [US1] Implement the mode-dispatched body edit + content reassembly in handler.ts. Switch on `input.mode`: `"append"` → `editedLines = applyAppend(lines, resolved, input.content)`; `"prepend"` → `applyPrepend(...)`; `"replace"` → `applyReplace(...)`. Reassemble: `editedContent = editedLines.join(lineEnding === "crlf" ? "\r\n" : "\n")`; preserve trailing newline — if `trailingNewline` is true AND `editedContent` does not end with `\n`/`\r\n`, append the appropriate line-ending; if `trailingNewline` is false AND `editedContent` ends with the line-ending, strip it. Depends on T012.
- [ ] T014 [US1] Implement the atomic write per ADR-009 §3 in handler.ts. Generate a uniquified tmp filename: `tmpPath = absPath + "." + process.pid + "." + Date.now() + ".tmp"` (PID + ms-timestamp avoids collision between concurrent calls). `await deps.fs.writeFile(tmpPath, editedContent, "utf8")`. Then `await deps.fs.rename(tmpPath, absPath)` — atomic same-volume rename per ADR-009. Wrap in try/catch — generic fs failures (ENOSPC, EACCES, EROFS, etc.) surface as `UpstreamError` with `code: "FS_WRITE_FAILED"` + `details.errno` (mapped from the underlying `NodeJS.ErrnoException`). The `EBUSY` / `EPERM` external-editor classification is layered on by US4 — in US1, those errnos surface as the generic `FS_WRITE_FAILED`. The Buffer size is `Buffer.byteLength(editedContent, "utf8")` — capture as `bytesWritten` for the output envelope. Depends on T013.
- [ ] T015 [US1] Implement the post-write `metadataCache` invalidation eval per ADR-009 §5 in handler.ts. After the rename succeeds, fire the bug-safe small-eval call (cohort parity with [src/tools/write_note/handler.ts](src/tools/write_note/handler.ts)'s post-write eval): `(async()=>{const f=app.vault.getFileByPath(p);if(f)await app.metadataCache.computeMetadataAsync(f);})()` via `invokeCli` with the small eval template. Wrap in try/catch — eval failure is non-fatal (the write already landed); log a warning via `deps.logger` and continue. Build the output envelope: `{ path: relPath, vault: input.vault ?? "<focused>", heading_path: input.heading_path, mode: input.mode, bytes_written: bytesWritten }`. Validate via `patchHeadingOutputSchema.parse(...)` and return. Depends on T014.
- [ ] T016 [US1] Implement the unit test cohort in [src/tools/patch_heading/handler.test.ts](src/tools/patch_heading/handler.test.ts) covering US1's surface. Mocked `fs.readFile` / `fs.writeFile` / `fs.rename` / `fs.realpath` / `vaultRegistry.resolveVaultPath` via the standard test pattern (cohort parity with [src/tools/write_note/handler.test.ts](src/tools/write_note/handler.test.ts)). Cases: **happy paths × 3 modes** — append against a TODO list note (verify the output bytes match the expected post-edit shape exactly; verify everything outside the targeted region is byte-identical to input per SC-002); prepend a lead-in under Notes; replace the Done body. **Body shapes** — empty body for append (content lands immediately after marker); body with child subtree for replace (only the direct body is swapped, child preserved); last-heading-in-note for append (content lands at end-of-file with trailing-newline preserved); adjacent-child-marker for prepend (content lands between the two markers). **Line-ending preservation** — CRLF input, LF input — both round-trip cleanly. **Trailing-newline preservation** — file ends with `\n` → output ends with `\n`; file without `\n` → output without. **PATH_ESCAPES_VAULT** — mocked `realpath` returns a path outside vault root → envelope is `PATH_ESCAPES_VAULT` + `pathEscapeAttempt` logger event asserted. **FS_WRITE_FAILED** — mocked `fs.readFile` throws `ENOENT` → envelope names note-not-found; mocked `fs.writeFile` throws `ENOSPC` → envelope names errno. **Success envelope** — `path` echoes input's `path`/`file`; `vault` echoes input's vault; `heading_path` echoes input; `mode` echoes input; `bytes_written` equals the byte count passed to `fs.writeFile`. **Read-only verification of unmodified content** — for each placement-mode test, after running the handler, assert `fs.writeFile.mock.calls[0][1]` (the content actually written) substring-matches every region of the original outside the targeted reach byte-for-byte. **Frontmatter preservation (FR-016)** — dedicated happy-path fixture: note with a YAML frontmatter block (`---\ndate: 2026-05-21\ntags: [daily]\n---\n\n# Daily\n## Tasks\n### TODO\n- item\n`); invoke each of the three modes against `Daily#Tasks#TODO`; assert the leading `---\ndate: 2026-05-21\ntags: [daily]\n---\n\n` block is byte-identical in `fs.writeFile.mock.calls[0][1]` to the input file's leading bytes for every mode. **Repeat-invocation determinism (SC-004)** — invoke `executePatchHeading` twice in sequence against the same mocked fs fixtures (same canned `fs.readFile` payload, same input, fresh `vi.fn()` writeFile/rename spies per call so the second call's mocks return the same values); assert `JSON.stringify(result1) === JSON.stringify(result2)` AND `fs.writeFile.mock.calls[0][1] === fs.writeFile.mock.calls[1][1]` (byte-identical content written). **No-backup assertion (FR-024)** — across the full test cohort, assert that NO `fs.writeFile` call argv carries a `path` ending in `.bak`, `.backup`, or `.old` (the wrapper does not take backups before destructive `replace`). **No-streaming assertion (FR-025)** — assert the handler does not import `fs.createReadStream`, `fs.createWriteStream`, or `node:stream`; whole-file `readFile` / `writeFile` is the only IO primitive consumed. (This is a static-import assertion via the test file's import inventory; if the runtime test fixture cannot inspect imports, fall back to the per-call invariant: `fs.createReadStream` and `fs.createWriteStream` mocks remain uncalled across every test path.) **metadataCache eval** — mocked `invokeCli` for the post-write eval is called with the expected small-template; eval failure logs a warning and does NOT throw. Depends on T009..T015.
- [ ] T017 [US1] Implement the factory + descriptor in [src/tools/patch_heading/index.ts](src/tools/patch_heading/index.ts). Export `createPatchHeadingTool(deps: RegisterDeps): RegisteredTool` factory matching the pattern of [src/tools/write_note/index.ts](src/tools/write_note/index.ts). `RegisterDeps = ExecuteDeps`. Use `registerTool` from `../_register.js`. Export the canonical name as `export const PATCH_HEADING_TOOL_NAME = "patch_heading"`. Export a one-paragraph `PATCH_HEADING_DESCRIPTION` string summarising the spec's user-facing intent: when to reach for the tool, the three placement modes with body semantics per mode, the heading_path locator with `#` separator + 2-segment minimum + in-text `#` exclusion, the first-match-wins on duplicate sibling rule, the success envelope echo for write-verification, the five typed error states (`HEADING_NOT_FOUND`, `HEADING_RACE`, `EXTERNAL_EDITOR_CONFLICT`, `INVALID_HEADING_PATH`, `EMPTY_CONTENT`), and a pointer to `help({ tool_name: "patch_heading" })` for full docs (ADR-005 progressive disclosure). Depends on T003, T009.
- [ ] T018 [US1] Implement the descriptor + registration tests in [src/tools/patch_heading/index.test.ts](src/tools/patch_heading/index.test.ts). Cases: descriptor shape matches the project convention (parity with [src/tools/write_note/index.test.ts](src/tools/write_note/index.test.ts)); tool name is exactly `"patch_heading"`; the published `inputSchema` (via the centralised `zodToJsonSchema` step in `registerTool`) matches the contract in [contracts/input.schema.json](contracts/input.schema.json); registration via the factory wires the handler with the supplied deps. Depends on T017.
- [ ] T019 [US1] Wire `createPatchHeadingTool` into the boot spine at [src/server.ts](src/server.ts). Add the import line in alphabetical order with the existing tool imports (alongside `createPathsTool`, `createPatternSearchTool`, `createPropertiesTool`, etc.). Add the factory call in the `createTool` registration list (matches the convention used by every other tool). Boot-spine ownership of `createQueue()`, `createLogger()` per Constitution Principle I — handler must not reach back into the composition root at runtime. Depends on T017.
- [ ] T020 [US1] Append the `patch_heading` entry to [src/tools/_register-baseline.json](src/tools/_register-baseline.json) — the registry-stability baseline fixture (BI-031 / FR-018). The entry includes the tool name + the published `inputSchema` (the zod-to-json-schema-converted shape). Update the entry count and verify the baseline test in [src/tools/_register-baseline.test.ts](src/tools/_register-baseline.test.ts) passes. Depends on T019.

**Checkpoint**: US1 ships the happy-path MVP for `target_mode: "specific"`. `npm run lint && npm run typecheck && npm run build && vitest run` all pass. Quickstart examples 1–4 can run live against the authorised test vault. The tool is reachable via MCP clients. Active-mode (T024), race detection (T021–T023), and external-editor-conflict classification (T025–T026) are still pending — those errors currently surface as raw `CLI_REPORTED_ERROR/HEADING_NOT_FOUND` (T012's throw site, no race detection) or `FS_WRITE_FAILED/EBUSY` (T014's catch-all, no editor-conflict classification).

---

## Phase 4: User Story 2 - Distinguish unresolvable-target failures through typed errors (Priority: P2)

**Goal**: Layer the diagnostic-error classification on top of US1's handler so the caller can branch on `details.code` to distinguish `HEADING_NOT_FOUND` (heading path doesn't resolve), `INVALID_HEADING_PATH` (already caught at schema layer by T003 + T006 — no new handler work needed), and `HEADING_RACE` (heading hierarchy along the path changed between resolve and pre-write re-walk per FR-019). The wrapper-side race detection runs immediately before `fs.writeFile`.

**Independent Test**: Run quickstart.md failure-mode examples "Heading not found", "Malformed heading path (single segment)", and "Heading-text race" against the authorised test vault. Verify each error envelope's top-level `code` + `details.code` matches [contracts/errors.md](contracts/errors.md) exactly, AND the file on disk is byte-identical to its pre-call state for every error path.

The schema-layer validation tasks (T003 / T006) already cover `INVALID_HEADING_PATH` and `EMPTY_CONTENT` at the input-validation boundary. US2's tasks layer in the post-validation error classification: HEADING_NOT_FOUND (already thrown by T012) gets dedicated test coverage; HEADING_RACE is new wrapper logic + tests.

### Implementation for User Story 2

- [ ] T021 [US2] Implement the pre-write race detection in handler.ts per FR-019 + research.md R4. After T013 builds `editedContent` but BEFORE T014's `fs.writeFile`: re-read the file with `currentBytes = await deps.fs.readFile(absPath, "utf8")` — captures any concurrent modification since T012's read. Re-parse the segments and re-walk: `currentResolved = walkHeadings(currentBytes, segments)`. Race conditions:
  - If `currentResolved === null`: the path no longer resolves at all. Throw `UpstreamError` with `code: "CLI_REPORTED_ERROR"` + `details.code: "HEADING_RACE"` + `details.heading_path: input.heading_path` + `details.path: relPath` + `details.original_identity: originalIdentity` + `details.current_identity: null`.
  - If `currentResolved !== null` but `resolveHeadingIdentity(currentResolved)` does NOT byte-equal `originalIdentity`: throw `HEADING_RACE` with `details.current_identity: resolveHeadingIdentity(currentResolved)`.
  - If the identity matches but `currentBytes !== originalContent` (concurrent body edit elsewhere in the file): the race comparator passes per the R4 rationale (the heading itself is unchanged); proceed with the write but use the freshly-edited `editedContent` derived from `originalContent` — the conflict will be settled by the substrate's atomic-rename last-write-wins outcome per R5. Inline-comment this branch explicitly so future readers see the deliberate choice.
  - If `currentBytes === originalContent`: no race, no concurrent edit; proceed unchanged.
  The detection runs synchronously between T013 and T014; the time window between the re-read and the rename is small but non-zero, accepting a small residual race (mitigated by the substrate's atomic rename). Depends on T013, T014.
- [ ] T022 [US2] Extend handler.test.ts with the US2 HEADING_NOT_FOUND test cohort. Cases: heading path resolves to no heading (mocked file content has no `## Sub` under `# Top` but the caller asks for `Top#Sub`) → envelope is `CLI_REPORTED_ERROR + details.code: "HEADING_NOT_FOUND" + details.heading_path + details.path`; ancestor not found (caller asks for `Top#Sub` but only `# Other` exists) → same envelope; right-text-wrong-parent (caller asks for `Top#Sub` against a note where `## Sub` exists but under `# Other`, not `# Top`) → HEADING_NOT_FOUND (the parent chain didn't match, per FR-001); **fs unchanged** — assert `fs.writeFile.mock.calls.length === 0` and `fs.rename.mock.calls.length === 0` for every HEADING_NOT_FOUND test path per FR-022. Depends on T012, T016.
- [ ] T023 [US2] Extend handler.test.ts with the US2 HEADING_RACE test cohort per research.md R4 + the T021 race detection logic. Cases: **leaf renamed between resolve and re-walk** — fixture: T012's `fs.readFile` returns content where `## TODO` exists; T021's re-read `fs.readFile` returns content where `## TODO` has been renamed to `## Pending` → envelope is `HEADING_RACE` with `original_identity.markerLineText: "## TODO"` and `current_identity: null` (path no longer resolves). **Ancestor renamed** — fixture: initial read has `# Daily / ## Tasks`; re-read has `# Renamed / ## Tasks` → HEADING_RACE with `current_identity: null` (path `Daily#Tasks` no longer resolves). **Rank changed in place** — fixture: initial read has `## TODO`; re-read has `### TODO` at the same offset → HEADING_RACE with `original_identity.rank: 2`, `current_identity.rank: 3`. **Unrelated body edit (NOT a race)** — fixture: initial read has `## TODO\n- a` then `## Done`; re-read has `## TODO\n- a\n- b` then `## Done` (someone appended a bullet between the resolve and the race-check) → the identity comparator passes (heading text + rank + parent unchanged); the handler proceeds with the write using the `editedContent` derived from `originalContent`; the last-write-wins outcome is the documented behaviour per R5. **fs unchanged** for every HEADING_RACE test path — assert `fs.writeFile` and `fs.rename` are NOT called. Depends on T021.

**Checkpoint**: US2 ships the heading-resolution + race-detection diagnostic surface. Quickstart failure-mode examples for HEADING_NOT_FOUND + INVALID_HEADING_PATH (single-segment, empty-segment, etc.) + HEADING_RACE pass. The schema-layer validation (T006) already covered EMPTY_CONTENT. Each error path leaves the file on disk byte-identical to pre-call state per FR-022.

---

## Phase 5: User Story 3 - Operate on the focused note (Priority: P3)

**Goal**: Add the active-mode focused-note locator per FR-007 + FR-008. The wrapper resolves the currently-focused note via a small bug-safe `obsidian eval` (cohort parity with `write_note`'s active mode per ADR-009 §6) and applies the patch to that note. When no note is focused, surface `ERR_NO_ACTIVE_FILE` per the cohort convention.

**Independent Test**: Run quickstart.md example 5 ("Append against the focused note (active mode)") and the failure-mode example "No focused note (active mode failure)" against the authorised test vault. Verify the success path lands the patch on the focused note's named heading and that the absence-of-focus path surfaces `ERR_NO_ACTIVE_FILE` without touching the filesystem.

This story layers on top of US1 + US2 by adding the active-mode branch to T010 + T015's locator resolution. The body-edit + race detection + write logic is shared unchanged with the specific-mode path.

### Implementation for User Story 3

- [ ] T024 [US3] Implement the active-mode focused-file resolution in handler.ts per ADR-009 §6. Branch in T010's locator-resolution code: when `input.target_mode === "active"`, fire the bug-safe small-eval via `invokeCli` with command `eval` + the cohort-standard template `(()=>({base: app.vault.adapter.basePath, path: app.workspace.getActiveFile()?.path ?? null}))()` (cohort parity with [src/tools/write_note/handler.ts:114-127](src/tools/write_note/handler.ts)). Parse the eval response: on parse failure throw `UpstreamError` with `code: "CLI_REPORTED_ERROR"` + `details.stage: "json-parse"`; on envelope-shape failure throw with `details.stage: "envelope-parse"`. When `parsed.path === null`, throw `UpstreamError` with `code: "ERR_NO_ACTIVE_FILE"` + message naming the remediation: "No active file in Obsidian. Open a note in the editor, or call patch_heading with target_mode=specific + vault + file/path." (byte-stable with write_note's message). On success: `vaultRoot = parsed.base`; `relPath = parsed.path`. Also resolve the vault DISPLAY NAME from the vault registry by reverse-lookup on `vaultRoot` for the success envelope's `vault` field — fall back to the literal vaultRoot string if no display-name mapping is found (the registry's reverse-lookup is added in T024a). The rest of the handler (Layer 2 path check, read, walk, race, edit, write) proceeds unchanged with the resolved `vaultRoot` + `relPath`. Depends on T010.
- [ ] T024a [US3] [P] If the existing `vault-registry/` does not already expose a reverse-lookup (basePath → displayName), add `resolveVaultDisplayName(basePath: string): string | null` to [src/vault-registry/index.ts](src/vault-registry/index.ts). The lookup walks the cached vault list and returns the display name whose `basePath` matches; returns `null` when no match (the focused vault may not be a named entry in the registry — the wrapper falls back to the basePath literal). Verify with a co-located test in [src/vault-registry/index.test.ts](src/vault-registry/index.test.ts). If the helper already exists, this task is a no-op (link the existing function in the handler instead). Depends on T024.
- [ ] T025 [US3] Extend handler.test.ts with the US3 active-mode test cohort. Cases: **happy path** — mocked `invokeCli` returns `{stdout: '{"base":"/path/to/Knowledge","path":"Daily Notes/2026-05-21.md"}'}` for the focused-file eval; the rest of the handler chain (Layer 2 path check, read, walk, edit, write) runs against the resolved path; envelope's `vault` echoes the resolved display name; envelope's `path` echoes the resolved relative path. **No focused file** — mocked eval returns `{stdout: '{"base":"/path/to/Knowledge","path":null}'}` → envelope is `ERR_NO_ACTIVE_FILE`; assert NO fs.readFile / fs.writeFile / fs.rename calls (file system untouched per FR-008). **Active-mode + heading happy path** — full integration: active mode resolves the focused file, the patch lands at the named heading, the success envelope echoes everything correctly. **Eval parse failure** — mocked eval returns invalid JSON → `CLI_REPORTED_ERROR + details.stage: "json-parse"`. **Eval envelope shape failure** — mocked eval returns `{stdout: '{"unexpected":"shape"}'}` → `CLI_REPORTED_ERROR + details.stage: "envelope-parse"`. Depends on T024, T024a.

**Checkpoint**: US3 ships the active-mode focused-note locator. Quickstart example 5 + the "No focused note" failure example pass. The cohort's active-mode TOCTOU posture (per the memory-feedback rule: "project-wide concerns get ADRs applied uniformly, not per-tool retrofits") inherits unchanged.

---

## Phase 6: User Story 4 - Survive concurrent and external edits without corruption (Priority: P4)

**Goal**: Layer the external-editor-conflict classification on top of T014's fs error catch. When the substrate's `fs.rename` (or `fs.writeFile`) throws with an errno indicating the target is held open by an editor with non-shared-delete access (Windows `EBUSY` / `EPERM`; certain `EACCES` patterns on POSIX), surface `EXTERNAL_EDITOR_CONFLICT` with `details.reason: "file-locked"` per FR-021. Other fs failures remain `FS_WRITE_FAILED` (T014's catch-all).

**Independent Test**: Run quickstart.md failure-mode example "External editor conflict (Windows)" against the authorised test vault on Windows — open the target note in an external editor with unsaved changes, invoke patch_heading, verify the envelope is `CLI_REPORTED_ERROR + details.code: "EXTERNAL_EDITOR_CONFLICT" + details.reason: "file-locked" + details.errno: "EBUSY"`. On platforms where the substrate cannot detect (Linux / macOS), verify the edit lands and document the detection-capability caveat per FR-021.

This story is the final FR mapping. Concurrent-edits-without-loss (FR-020) is already covered by T014's atomic temp-then-rename (cohort substrate per ADR-009 §3); no additional work needed — the last-write-wins outcome satisfies FR-020 per research.md R5.

### Implementation for User Story 4

- [ ] T026 [US4] Implement the external-editor-conflict classification in handler.ts per FR-021 + research.md R6. Extend T014's fs catch-all: when the caught error's `errno` is one of `"EBUSY" | "EPERM" | "EACCES"` AND `code === "EBUSY" | "EPERM"`, classify as `UpstreamError` with `code: "CLI_REPORTED_ERROR"` + `details.code: "EXTERNAL_EDITOR_CONFLICT"` + `details.reason: "file-locked"` + `details.path: relPath` + `details.errno: error.errno`. The exact errno→classification pattern is finalised at /speckit-implement T0 probe time (R11 item 1 — capture the actual errno shape Windows produces when Obsidian holds the file). Other errnos (`ENOSPC`, `EROFS`, etc.) fall through to T014's generic `FS_WRITE_FAILED`. The `"unsaved-changes"` sub-reason is reserved for a future detection mechanism per ADR-015's multi-state-from-day-one preference but is currently never emitted by the wrapper (no detection mechanism exists yet) — document this in the inline comment so future readers see the deliberate placeholder. Depends on T014.
- [ ] T027 [US4] Extend handler.test.ts with the US4 EXTERNAL_EDITOR_CONFLICT test cohort. Cases: **fs.rename throws EBUSY** — mocked `fs.rename` throws `Object.assign(new Error("EBUSY: resource busy or locked"), { errno: -4082, code: "EBUSY" })` → envelope is `CLI_REPORTED_ERROR + details.code: "EXTERNAL_EDITOR_CONFLICT" + details.reason: "file-locked" + details.errno: "EBUSY" + details.path`. **fs.writeFile throws EBUSY on the tmp path** — same classification. **fs.rename throws EPERM** — same classification (some Windows shares-modes use EPERM). **fs.rename throws ENOSPC** — falls through to `FS_WRITE_FAILED` (the disk-full case is NOT editor-conflict). **fs unchanged after failure** — assert the original `absPath` is NOT renamed-to; assert any `.tmp` file the test created in a real-fs fixture is cleaned up (or, for mocked fs, that no `fs.rename` succeeded). **Concurrent-write last-write-wins (FR-020)** — fixture: two parallel patch_heading calls against the same note; mocked `fs.rename` simulates the OS atomic semantics by serialising the two renames (returning successful + observing the order); assert both succeed with no half-written file at any intermediate observable instant; document that the post-state reflects the LATER rename's content per the substrate's last-write-wins outcome. Depends on T026.

**Checkpoint**: US4 ships the external-editor-conflict classification. Quickstart's "External editor conflict (Windows)" example passes on Windows; the Linux / macOS detection-capability caveat is documented in tool help. Every error path listed in [contracts/errors.md](contracts/errors.md) is now tested. Concurrent-write last-write-wins (FR-020) inherits unchanged from the substrate.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: T0 live-vault captures, docs, README update, baseline cleanup, quality-gate final pass.

- [ ] T028 [P] Run T0 live-vault probes per [research.md §R11](research.md) against the authorised test vault (per `.memory/test-execution-instructions.md`). Capture: (1) Windows EBUSY/EPERM errno shape for `fs.rename` against a note held open by Obsidian with unsaved changes — update T026's classification table if the actual errno differs from EBUSY. (2) Trailing-newline preservation across the substrate — patch a note that ends with `\n` and a note that does not; verify both conventions survive. (3) CRLF preservation — patch a note with CRLF endings; verify the output retains CRLF. (4) Active-mode focused-file resolution for an unsaved note (brand-new note opened in Obsidian without saving) — verify the focused-file eval resolves a path or surfaces a sensible error. (5) MetadataCache invalidation effectiveness post-patch — patch a heading then immediately invoke `read_heading` on the same heading; verify the post-patch body is visible (no stale cache). Record probe results inline in [research.md §R11](research.md) and update any handler.ts classification tables or wrapper logic as needed. Probes are write-modifying — use the scratch subdirectory per test-execution-instructions.md and clean up afterwards. Depends on T020.
- [ ] T029 [P] Fill in the docs page at [docs/tools/patch_heading.md](docs/tools/patch_heading.md) — full progressive-disclosure documentation per ADR-005. Required sections (mandatory; each must be present at PR-time and is gated by inspection):
  1. **Overview** — one-paragraph intent statement.
  2. **When to use this tool** — vs. `write_note` (full-note rewrite), `find_and_replace` (text-level), `outline`/`read_heading` (read-side).
  3. **Input schema** — cross-reference to [contracts/input.schema.json](contracts/input.schema.json); enumerate every field with type + constraints.
  4. **Three placement modes** — with body-semantics worked examples drawn from [quickstart.md](quickstart.md) for each of `append`, `prepend`, `replace`. Must include the asymmetric empty-content rule per FR-018a (replace accepts empty; append/prepend reject empty).
  5. **The heading_path locator** — `#` segment separator per FR-004; two-segment minimum per FR-002; in-text `#` exclusion per FR-005; first-match-wins on duplicate siblings per FR-006 + SC-006.
  6. **Active-mode focused-note locator** — `target_mode: "active"` shape; cohort parity with `write_note`; ERR_NO_ACTIVE_FILE remediation.
  7. **Output envelope** — `{ path, vault, heading_path, mode, bytes_written }`; meaning of each field.
  8. **Error states** — full `details.code` enumeration cross-referenced to [contracts/errors.md](contracts/errors.md): HEADING_NOT_FOUND, HEADING_RACE, EXTERNAL_EDITOR_CONFLICT, INVALID_HEADING_PATH (5 sub-reasons), EMPTY_CONTENT (2 sub-reasons), reused VAULT_NOT_FOUND / ERR_NO_ACTIVE_FILE / PATH_ESCAPES_VAULT / FS_WRITE_FAILED.
  9. **Detection-capability caveats (FR-021 platform variance)** — MANDATORY explicit subsection. Must spell out: on Windows, the substrate signals EXTERNAL_EDITOR_CONFLICT via `fs.rename` EBUSY/EPERM when an editor holds the file with non-shared-delete access; on Linux/macOS, the substrate has no analogous signal for in-memory-only dirty state, so the patch lands on disk and the external editor sees a refreshed file on next focus. Callers automating against multi-platform deployments must plan around the divergence. This subsection is the source for the agent-readable detection-capability statement the spec's FR-021 mandates.
  10. **Worked-example quickstart snippets** — at least one per happy-path mode plus at least one per typed error.

  The `help({ tool_name: "patch_heading" })` MCP call returns this doc per the cohort convention. Depends on T017.
- [ ] T030 [P] Update the [README.md](README.md) tool inventory to include `patch_heading` with its one-line summary. Place the entry in the read-and-write tools section alongside `write_note`, `find_and_replace`, etc., in alphabetical order. No Attributions section changes (no new upstream code lifted per plan.md's Principle V evidence). Depends on T029.
- [ ] T031 Run the full quality-gate suite per [.specify/memory/constitution.md §Development Workflow](/.specify/memory/constitution.md): `npm run lint && npm run typecheck && npm run build && vitest run`. All four MUST pass with zero warnings. Verify the aggregate statements-coverage threshold in [vitest.config.ts](vitest.config.ts) still passes (the new module ships with ≥ 90% statement coverage by virtue of the per-helper + per-handler test cohort). If any gate fails, fix and re-run; do not bypass via `--no-verify` or coverage adjustment without an explicit user authorisation. Depends on T028.

**Checkpoint**: BI-040 ships. Every FR from spec.md is covered by at least one task; every error envelope in contracts/errors.md is tested; every SC in spec.md is exercised by at least one test or quickstart example. The Constitution Compliance checklist in the PR description lists Y on Principles I-V + ADR-003 + ADR-009 + ADR-015; N/A on ADR-010 + ADR-013 + ADR-014.

---

## Dependencies

Story-completion dependencies (which story can begin once which is complete):

- **US1 (P1)** depends on Setup (T001-T002) + Foundational (T003-T008). MVP shipped at T020.
- **US2 (P2)** depends on US1 (T009-T020 must complete first — US2 layers race detection on top of the read-walk-edit-write chain).
- **US3 (P3)** depends on US1 (extends T010 with the active-mode branch). Independent of US2.
- **US4 (P4)** depends on US1 (extends T014's fs catch-all). Independent of US2 and US3.
- **Polish (Phase 7)** depends on every prior phase.

Within a story, task IDs are dependency-ordered (lower ID = earlier execution). [P]-marked tasks within the same phase can run in parallel.

## Parallel execution opportunities

- **Phase 1** — T001 and T002 are independent files; can run in parallel.
- **Phase 2** — T003 (schema), T004 (heading-walk), T005 (body-edit) are three independent production files; can run in parallel. Their respective tests (T006, T007, T008) each depend only on their own production file; can run in parallel pairwise once the production file lands.
- **Phase 3 (US1)** — T009 sets up the handler skeleton; T010-T015 are sequential within the handler (each builds on the previous step in the read-modify-write chain). T017 (factory + descriptor) can be authored in parallel with T013-T015 if it does not import the handler's internals (descriptor only).
- **Phase 7 (Polish)** — T028, T029, T030 are independent files; can run in parallel.

## Implementation strategy

**MVP first**: ship US1 (T001-T020) as a contained shippable slice. The tool is reachable via MCP clients, supports all three placement modes, and lands the happy paths correctly. Error states surface as raw `CLI_REPORTED_ERROR/HEADING_NOT_FOUND` or `FS_WRITE_FAILED` without the full diagnostic discrimination — sufficient for the first user-visible behaviour but not the full contract.

**Incremental layering**: US2 (T021-T023) adds race detection; US3 (T024-T025) adds active mode; US4 (T026-T027) adds editor-conflict classification. Each of US2/US3/US4 is independently mergeable once US1 lands.

**Polish last**: T028-T031 run after all four user stories ship; T0 probe results may surface adjustments to T026's classification table or T021's race-check semantics.

## Constitution Compliance checklist (PR-time)

- [ ] Principle I (Modular Code Organization): Y — per-surface module at `src/tools/patch_heading/`, one-directional imports.
- [ ] Principle II (Public Surface Test Coverage): Y — five `*.test.ts` files co-located with five production files; happy-path + failure-or-boundary coverage per Principle II.
- [ ] Principle III (Boundary Input Validation with Zod): Y — `patchHeadingInputSchema` is sole source of truth for both `inputSchema` publication and runtime parse.
- [ ] Principle IV (Explicit Upstream Error Propagation): Y — all failures route through `UpstreamError`; zero new top-level codes (seventeen-tool zero-new-codes streak post-BI-040).
- [ ] Principle V (Attribution & Layered Composition): Y — every file carries `// Original — no upstream.` header; no new upstream code lifted.
- [ ] ADR-010 (Typed Tool Names Mirror Upstream CLI Subcommand): N/A — no upstream `note:patch` subcommand; fs-direct lineage with `write_note`.
- [ ] ADR-013 (Plugin-Namespace Tool Naming Convention): N/A — no plugin dependency.
- [ ] ADR-014 (Plugin-Backed Typed Tools Runtime-Dependency Pattern): N/A — no plugin dependency.
- [ ] ADR-015 (Sub-Discriminators via `details.reason` for Multi-State Error Codes): Y — three new multi-state `details.code` pairs (`INVALID_HEADING_PATH`, `EMPTY_CONTENT`, `EXTERNAL_EDITOR_CONFLICT`) all carry `details.reason` enumerations per ADR-015.
